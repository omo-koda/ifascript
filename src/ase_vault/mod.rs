/// Ase Vault — ese Ifá corpus loader
/// Returns the ese (verse) for the given tier and odu_id.
/// Currently stubbed; corpus to be loaded from data/ in future.
pub fn get_ese(tier: u8, odu_id: u16) -> Option<String> {
    let _ = (tier, odu_id);
    None
}
